import { expect } from 'chai'
import activate from '../src/discovery/Discovery.hy.js'

describe('Survey Reporter', ()=>{

    describe('onUserListChanged', ()=> {
        it('should be emmited when user list changes')
    })

    describe('query', ()=>{
        it('should return registered users when teh hyperty was initialized')

        it('should return registered users when the hyperty was initilized and any other hyperty registered before')
    })
})
